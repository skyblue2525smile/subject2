  <p id="notice"></p>
  <h1>Books</h1>

  <% @books.each do |book| %>
   <span>
     <%= link_to book.title, book_path(book.id) %>
   </span>
  <% end %>

  <table>

     <thead>
      <tr>
        <th>Title</th>
        <th>Body</th>
        <th colspan="3"></th>
      </tr>
     </thead>



     <tbody>
      <tr>
        <td>title</td>
        <td>body</td>
        <td>Show</td>
        <td>Edit</td>
        <td>Destroy</td>
      </tr>
     </tbody>
    </table>

    <h2>New book</h2>
    <% if @book.errors.any? %>
      <%= @book.errors.count %>件のエラーが発生しました
      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    <% end %>

    <%= form_with model: @book, url: '/books', method: :post do |f| %>
      <input name="utf-8" type="hidden" value="✓">
      <input type="hidden" name="authenticity_token" value="jatZ4ew4Nw36OgB3Q3j7rsWp24GLAsZNmFLhzs5QDZWm+pE1TxBGmfnf6YLLdwC/NcasULagIza1DglDH85f4w==">
      <div class="field">
         <label for="book_title">Title</label>
         <input class="book_title" type="text" name="book[title]" id="book_title">
      </div>
      <div class="field">
       <label for="book_body">Body</label>
       <textarea class="book_body" name="book[body]" id="book_body"></textarea>
      </div>
         <div class="actions">
         <input type="submit" name="commit" value="Create Book" data-disable-with="Create Book">
      </div>
    <% end %>




